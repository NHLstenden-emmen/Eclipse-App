<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="INF1D.eclipse">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Eclipse"
            android:usesCleartextTraffic="true"
            tools:ignore="AllowBackup">

        <activity
                android:name=".settings.widgethandler.widget.widgetSettingsActivity"
                android:label="@string/title_activity_widget_settings">
        </activity>
        <activity
                android:name=".SplashScreen"
                android:theme="@style/LaunchTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".settings.MirrorSettingsActivity"/>
        <activity android:name=".settings.widgethandler.WidgetHandlerActivity"/>
        <activity android:name=".setup.SetupActivity" android:theme="@style/Theme.Eclipse.NoActionBar"/>
        <activity android:name=".TempUserSettings" />
    </application>

</manifest>
